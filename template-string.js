const nombre = 'Juan';
const real = 'Ikaro';

const normal = nombre + ' ' + real;
const template = `${nombre} ${normal}`;

console.log(template);
console.log(normal);

const html = `
<h1>hola</h1>
<p>Mundo</p>
`;

console.log(html);